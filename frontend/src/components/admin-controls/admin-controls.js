import { Link } from 'react-router-dom';
import styles from './admin-controls.module.css';

export const AdminControls = (id) => {
	return (
		<div className={styles.controls}>
			<Link to></Link>
		</div>
	);
};
